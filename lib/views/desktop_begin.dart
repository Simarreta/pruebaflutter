import 'package:flutter/material.dart';

class DesktopBegin extends StatelessWidget {
  const DesktopBegin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("data");
  }
}